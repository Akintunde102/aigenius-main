/**
 * Re-exports from `frontend/src/shared/tool-display-names.ts` (must match backend copy).
 */
export {
    TOOL_DISPLAY_NAMES,
    getToolDisplayName,
    getToolActivityHint,
} from '@shared/tool-display-names';
