/**
 * When true, the previous filter/sort UI (modalities, web search, tools, cost dropdown) is shown.
 * When false, the new filter/sort UI is shown: hidden by default, toggled via "Filter & sort";
 * only provider (first 5 major labs) and sort options. Context filtering is disabled.
 */
export const SHOW_LEGACY_FILTERS = false;
