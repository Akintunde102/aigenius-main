export default function HomePageWrapper() {
  // The layout.tsx renders AuthenticatedChatPage, so the page itself is empty
  // but exists to fulfill Next.js routing requirements for the '/' route.
  return null;
}
